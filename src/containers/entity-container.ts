import { Container, Sprite } from 'pixi.js';
import { IScene } from '../shared/scene-manager';

export class EntityContainer extends Container implements IScene {
    
    update() {

    }

    resize() {

    }
}